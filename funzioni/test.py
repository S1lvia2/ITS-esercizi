# def sum_above_threshold(numbers: list[int], threshold) -> int: 
#     sum = 0 
#     for i in numbers: 
#         if i > threshold: 
#             sum = sum +i 
#     return sum

# def rotate_left(elements: list, k: int) -> list: 
#     n = len(elements)
#     k = k % n  
#     elements[:] = elements[k:] + elements[:k]  
#     return elements

# def even_odd_pattern(numeri: list[int]) -> list[int]:
#     pari = [n for n in numeri if n % 2 == 0]
#     dispari = [n for n in numeri if n % 2 != 0]
#     return pari + dispari



    # while n > 1:
    #     while n % divisore == 0:
    #         fattori.append(divisore)
    #         n //= divisore
    #     divisore += 1

    # return fattori

# def somma_elementi(x: list[int], y: list[int]) -> list[int]:
#     if len(x) != len(y):
#         raise ValueError("Le due liste devono avere la stessa lunghezza.")
#     risultato = []
#     for i in range(len(x)):
#         risultato.append(x[i] + y[i])
#     return risultato

# def even_odd_pattern(numeri: list[int]) -> list[int]:
#     pari = [n for n in numeri if n % 2 == 0]
#     dispari = [n for n in numeri if n % 2 != 0]
#     return pari + dispari

# def even_odd_pattern(numeri: list[int]) -> list[int]:
#     pari = [n for n in numeri if n % 2 == 0]
#     dispari = [n for n in numeri if n % 2 != 0]
#     return pari + dispari


# dd